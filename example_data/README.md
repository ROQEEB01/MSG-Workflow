# Example data

Raw FASTQ/BAM files are not included because sequencing datasets are large.

The configuration templates in `config/` illustrate the required file formats. A future small synthetic example dataset can be added here without changing the workflow.
